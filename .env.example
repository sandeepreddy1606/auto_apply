# Copy this file to `.env` and fill in your values. `.env` is gitignored.

# PostgreSQL connection string (e.g. from Neon or Supabase). When set, the app
# stores applications/companies in Postgres; when unset, it uses local SQLite.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
